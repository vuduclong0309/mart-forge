{
  "hooks": {
    "SessionStart": [
      {
        "command": "echo '=== mart-forge plugin loaded ===' && echo '' && echo 'Kimball DWH scaffolding framework.' && echo 'Lifecycle: [A0] Source Discovery → [A] BRD → [B] TDD → sign-off → [C] Scaffold → [D] DQC → [E] Dashboard' && echo '' && echo 'Skills available:' && echo '  /using-mart-forge   — Detect phase and route (start here)' && echo '  /source-discovery   — Vet candidate sources from a WIKI-like doc (Phase A0)' && echo '  /mart-brd           — Produce a BRD for a new Kimball mart (Phase A)' && echo '  /mart-tdd           — Generate a TDD with column-level calc specs (Phase B)' && echo '  /mart-bootstrap     — Scaffold a mart from mart.yml; runs dbt compile to verify (Phase C)' && echo '  /dqc-audit          — Audit DQC coverage (Phase D)' && echo '  /schema-evolve      — Propagate column additions' && echo '  /mart-review        — Production-readiness review' && echo '' && echo 'Starting from a WIKI? Run /source-discovery first, then /mart-brd.' && echo 'Key constraints: no SELECT *, provenance on every ODS, all 8 DQC controls required.' && echo 'Run /using-mart-forge to get started.'",
        "description": "Orient the agent to mart-forge methodology and available skills on session start"
      }
    ]
  }
}
